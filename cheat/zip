# Create zip file
zip archive.zip file1 directory/

#This was weird one: 
#On OSX had to add -r to add all files & dirs of the desired directory
zip -r archive.zip file1 directory/

# To list, test and extract zip archives, see unzip
cheat unzip
